# Bomba A11 / AV Electronic

GitHub Pages-ready version.

## Structure
- `index.html` — main application
- `assets/` — all images used by the application
- `.nojekyll` — disables Jekyll processing

Upload the contents of this folder to the repository root. GitHub Pages should be configured to publish from the branch/folder containing `index.html`.
